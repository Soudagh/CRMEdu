/**
 * Provides the core domain logic for the CRM system, including domain models, business rules, and data access.
 * <p>
 * The {@code enums} package contains enumerations used across the application to represent different states and roles. The {@code exception} package defines
 * custom exceptions to handle errors specific to business logic and data processing. The {@code model} package contains entity classes representing core
 * business objects, such as {@code Student}, {@code Tutor}, {@code Lesson}, etc. The {@code repository} package provides interfaces for data access and
 * persistence of the domain models. The {@code service} package contains business logic and service layer classes responsible for implementing system
 * behavior.
 * </p>
 */
package org.example.crmedu.domain;
